export {
  compressFromUrl,
  compressFromBlob,
  detectFormat,
  estimateSize,
  type CompressionOptions,
  type CompressionResult,
} from "./compress";

export type { CompressionSettings } from "./types";
